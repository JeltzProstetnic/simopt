<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:DiagramDesigner="clr-namespace:MatthiasToolbox.GraphDesigner.DiagramDesigner" xmlns:GraphDesigner="clr-namespace:MatthiasToolbox.GraphDesigner" xmlns:Converters="clr-namespace:MatthiasToolbox.Presentation.Converters">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Themes/Shared.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Resources -->
    <Converters:ColorToBrushConverter x:Key="ColorConverter"/>

    <ContextMenu x:Key="ConnectionContextMenu">
        <MenuItem Header="Delete" Command="{x:Static ApplicationCommands.Delete}">
            <MenuItem.Icon>
                <Image Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Images/Delete.png" Width="16"/>
            </MenuItem.Icon>
        </MenuItem>
        <Separator/>
        <MenuItem Header="Order">
            <MenuItem Header="Bring Forward" Command="{x:Static DiagramDesigner:DesignerCanvas.BringForward}">
                <MenuItem.Icon>
                    <Image Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Images/BringForward.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Bring To Front" Command="{x:Static DiagramDesigner:DesignerCanvas.BringToFront}">
                <MenuItem.Icon>
                    <Image Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Images/BringToFront.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Send Backward" Command="{x:Static DiagramDesigner:DesignerCanvas.SendBackward}">
                <MenuItem.Icon>
                    <Image Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Images/SendBackward.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Send To Back" Command="{x:Static DiagramDesigner:DesignerCanvas.SendToBack}">
                <MenuItem.Icon>
                    <Image Source="pack://application:,,,/MatthiasToolbox.GraphDesigner;component/Images/SendToBack.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
        <Separator/>
        <MenuItem Header="Options" Command="{x:Static GraphDesigner:GraphControl.ShowOptions}"/>
    </ContextMenu>

    <Style TargetType="{x:Type DiagramDesigner:Connection}">
        <Style.Resources>
            <!-- Style for the ConnectorAdorner thumbs -->
            <Style x:Key="ConnectionAdornerThumbStyle" TargetType="{x:Type Thumb}">
                <Setter Property="Width" Value="12"/>
                <Setter Property="Height" Value="12"/>
                <Setter Property="SnapsToDevicePixels" Value="true"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <TranslateTransform X="-6" Y="-6"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Rectangle Fill="#AADCDCDC" Stroke="DodgerBlue" StrokeThickness="1" RadiusX="0" RadiusY="0"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- Arrow Grid Style -->
            <Style x:Key="ArrowGridStyle" TargetType="Grid">
                <Setter Property="Width" Value="10"/>
                <Setter Property="Height" Value="10"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <TranslateTransform X="-5" Y="-5"/>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- base style for all arrow shapes -->
            <Style x:Key="ArrowSymbolBaseStyle" TargetType="Path">
                <Setter Property="Fill" Value="{StaticResource SolidBorderBrush}"/>
                <Setter Property="Stretch" Value="Fill"/>
            </Style>
            <!-- Arrow -->
            <Style x:Key="Arrow" TargetType="Path" BasedOn="{StaticResource ArrowSymbolBaseStyle}">
                <Setter Property="Data" Value="M0,0 8,4 0,8 Z"/>
            </Style>
            <!-- Diamond  -->
            <Style x:Key="Diamond" TargetType="Path" BasedOn="{StaticResource ArrowSymbolBaseStyle}">
                <Setter Property="Data" Value="M-5,0 0,-5 5,0 0,5 Z"/>
            </Style>
        </Style.Resources>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DiagramDesigner:Connection}">
                    <Canvas DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                            ContextMenu="{StaticResource ConnectionContextMenu}">
                        <Path Name="PART_ConnectionPath"
                              StrokeThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=LineThickness}"
                              Stroke="{Binding Path=Edge.BackgroundColor, Converter={StaticResource ColorConverter}}"
                              
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              StrokeDashArray="{Binding StrokeDashArray}"
                              SnapsToDevicePixels="True"
                              Data="{Binding PathGeometry}">
                            <!-- Fill="{Binding Path=Edge.BackgroundColor, Converter={StaticResource ColorConverter}}" -->
                        </Path>
                        <Path Name="PART_ConnectionPathSelection"
                              StrokeThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=LineHighlightThickness}"
                              Stroke="Transparent"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              StrokeDashArray="{Binding StrokeDashArray}"
                              SnapsToDevicePixels="True"
                              Data="{Binding PathGeometry}">
                        </Path>
                        <!--<Grid Style="{StaticResource ArrowGridStyle}"
                              Canvas.Left="{Binding AnchorPositionSource.X}"
                              Canvas.Top="{Binding AnchorPositionSource.Y}">
                            <Path Name="PART_SourceAnchorPath"/>
                            <Grid.LayoutTransform>
                                <RotateTransform Angle="{Binding AnchorAngleSource}"/>
                            </Grid.LayoutTransform>
                        </Grid>
                        <Grid Style="{StaticResource ArrowGridStyle}"
                              Canvas.Left="{Binding AnchorPositionSink.X}"
                              Canvas.Top="{Binding AnchorPositionSink.Y}">
                            <Path Name="PART_SinkAnchorPath"/>
                            <Grid.LayoutTransform>
                                <RotateTransform Angle="{Binding AnchorAngleSink}"/>
                            </Grid.LayoutTransform>
                        </Grid>-->

                        <!--Uncomment this to show default label text-->
                        <TextBlock Name="PART_Label" Width="100" Height="35"
                                   Text="{Binding Path=Edge.Name}" Foreground="{Binding Path=Edge.ForegroundColor, Converter={StaticResource ResourceKey=ColorConverter}}"
                                   Canvas.Left="{Binding LabelPosition.X}" Canvas.Top="{Binding LabelPosition.Y}">
                              <TextBlock.RenderTransform>
                                <TranslateTransform X="5" Y="5"/>
                              </TextBlock.RenderTransform>
                            </TextBlock>
                        
                        
                        <!-- Uncomment for line shadow -->
                        <!--<Canvas.BitmapEffect>
                            <DropShadowBitmapEffect Color="Gray" Direction="315" ShadowDepth="10" Softness="0" Opacity="0.1"/>
                        </Canvas.BitmapEffect>
                        -->
                    </Canvas>
                    <ControlTemplate.Triggers>
                        <!-- Source Anchor Styles-->
                        <!--
                        <DataTrigger Value="Arrow" Binding="{Binding RelativeSource={RelativeSource Self},Path=SourceArrowSymbol}">
                            <Setter TargetName="PART_SourceAnchorPath" Property="Style" Value="{StaticResource Arrow}"/>
                        </DataTrigger>
                        <DataTrigger Value="Diamond" Binding="{Binding RelativeSource={RelativeSource Self},Path=SourceArrowSymbol}">
                            <Setter TargetName="PART_SourceAnchorPath" Property="Style" Value="{StaticResource Diamond}"/>
                        </DataTrigger>
                        -->
                        <!-- Sink Anchor Styles-->
                        <!--
                        <DataTrigger Value="Arrow" Binding="{Binding RelativeSource={RelativeSource Self},Path=SinkArrowSymbol}">
                            <Setter TargetName="PART_SinkAnchorPath" Property="Style" Value="{StaticResource Arrow}"/>
                        </DataTrigger>
                        <DataTrigger Value="Diamond" Binding="{Binding RelativeSource={RelativeSource Self},Path=SinkArrowSymbol}">
                            <Setter TargetName="PART_SinkAnchorPath" Property="Style" Value="{StaticResource Diamond}"/>
                        </DataTrigger>
                        -->
                        <Trigger Property="IsMouseOver" Value="True"> <!-- Set the color of the stroke and the filling color when highlighted -->
                            <Setter TargetName="PART_ConnectionPath" Property="Stroke" Value="{StaticResource ResourceKey=ConnectionHoverBrush}" />
                            <!--<Setter TargetName="PART_ConnectionPath" Property="Fill" Value="{StaticResource ResourceKey=ConnectionHoverBrush}" />-->
                        </Trigger>
                        <!-- Connection Path Thickness-->
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=LineThickness}" Value="{x:Null}">
                            <Setter TargetName="PART_ConnectionPath" Property="StrokeThickness" Value="2"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=LineHighlightThickness}" Value="{x:Null}">
                            <Setter TargetName="PART_ConnectionPathSelection" Property="StrokeThickness" Value="10"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
